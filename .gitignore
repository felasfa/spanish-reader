node_modules/
.env
.netlify/
# Data files are stored on the VPS filesystem, not in git
# (writes would trigger Netlify deploys on every user action)
data/scroll-positions.json
data/reading-list.json
data/vocabulary.json
server/scroll-positions.json
